# @vue-stately/flags

Vue state baseline for global feature flag toggles used by migrated stately packages.

## Exports

* `enableTableNestedRows`
* `tableNestedRows`
* `enableShadowDOM`
* `shadowDOM`
